#include <iostream>
#include <vector>
#include <string>
using namespace std;
string largestTimeFromDigits(vector<int>& A)
{

}
int main()
{
        vector<int> nums = {2, 0, 6, 6};
        cout << largestTimeFromDigits(nums) << endl;
        return 0;
}
